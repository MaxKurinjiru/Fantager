when@dev:
  web_profiler:
    toolbar: true

  framework:
    profiler:
      collect_serializer_data: true
      collect: false
      collect_parameter: "profile"

when@test:
  framework:
    profiler:
      collect: false
      collect_serializer_data: true
