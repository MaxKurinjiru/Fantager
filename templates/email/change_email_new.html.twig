<!DOCTYPE html>
<html lang="{{ locale }}">
<head>
    <meta charset="UTF-8">
    <title>{{ 'email.change_email_new.subject'|trans([], 'messages', locale) }}</title>
    <style>
        body { font-family: sans-serif; background: #0f172a; color: #e2e8f0; margin: 0; padding: 40px 20px; }
        .container { max-width: 480px; margin: 0 auto; background: #1e293b; border-radius: 8px; padding: 32px; }
        h1 { color: #fbbf24; font-size: 22px; margin-top: 0; }
        p { color: #94a3b8; line-height: 1.6; }
        a.button { display: inline-block; margin: 16px 0; padding: 12px 28px; background: #f59e0b; color: #0f172a; font-weight: bold; border-radius: 6px; text-decoration: none; }
        .note { font-size: 12px; color: #64748b; }
    </style>
</head>
<body>
<div class="container">
    <h1>{{ 'email.change_email_new.title'|trans([], 'messages', locale) }}</h1>
    <p>{{ 'email.change_email_new.greeting'|trans({'%displayName%': user.displayName}, 'messages', locale) }}</p>
    <p>{{ 'email.change_email_new.body'|trans([], 'messages', locale) }}</p>
    <p>{{ 'email.change_email_new.body_confirm'|trans([], 'messages', locale) }}</p>
    <a href="{{ url('app_confirm_email_change_new', {token: token}) }}" class="button">{{ 'email.change_email_new.btn'|trans([], 'messages', locale) }}</a>
    <p class="note">{{ 'email.change_email_new.note'|trans([], 'messages', locale) }}</p>
</div>
</body>
</html>
